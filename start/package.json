{
  "name": "{{ appName }}",
  "version": "{{ version }}",
  "description": "My Carolina Website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name Here",
  "license": "ISC",
  "dependencies": {
    "carolina": "{{ version }}"
  },
  "devDependencies": {
    "chai": "*",
    "mocha": "*"
  }
}